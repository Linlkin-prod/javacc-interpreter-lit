func int add(int a, int b) {
    int result = a + b;
    return result;
}

func int subtract(int a, int b) {
    int result = a - b;
    return result;
}

proc displayNum(int n) {
    int x = n;
}

main() {
    int x = 10;
    int y = 20;
    bool flag = true;
    float value = 3.14;

    print("Hello, World!");
    print("the value of x is:");
    print(x);
    
    x = 15;
    print("before addition, y is:");
    print(y);
    y = add(x, 5);
    print("After addition, y is:");
    print(y);

    bool test = x > y;
    print("Is x greater than y?");
    print(test);
    
    if (flag) {
        x = x + 1;
    } else {
        x = x - 1;
    }
    
    while (x < 100) {
        x = x + 10;
    }
    print("Final value of x:");
    print(x);
}
